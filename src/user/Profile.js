import React from 'react';
import Base from '../core/Base';

const Profile = () => {
    return(
        <Base title=" Profile Page">
        <h1>User Profile</h1>
        </Base>
    );
} 

export default Profile;